import { Sparkles, Gem, Droplet, Scissors, Wand, Palette, Flower } from "lucide-react";
import type { LucideIcon } from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
  sparkles: Sparkles,
  gem: Gem,
  droplet: Droplet,
  scissors: Scissors,
  wand: Wand,
  palette: Palette,
  flower: Flower,
};
